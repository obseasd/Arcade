import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "V4 Hook · Create",
  description: "Create a token on the production V4 ArcadeHook launchpad.",
};

export default function V4HookCreateLayout({ children }: { children: React.ReactNode }) {
  return children;
}
